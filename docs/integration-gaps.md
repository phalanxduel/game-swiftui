# Integration Gaps

This file tracks upstream contract gaps or mismatches that affect the standalone SwiftUI client. Each entry includes evidence, impact, and a suggested upstream issue title so uncertainty stays explicit.

| Title | Area | Observed evidence in `../game` | Why it blocks or affects the Swift client | Recommended upstream issue title | Status |
| --- | --- | --- | --- | --- | --- |
| `gameState.result.preState` semantics appear incorrect on broadcast | websocket / state shape | `server/src/match-actor.ts` & `prestate-broadcast.test.ts` track `preState` before action application | Verified preState captures pre-action snapshot | `[contract] Clarify or fix PhalanxTurnResult.preState in WS gameState broadcasts` | ✅ RESOLVED |
| `/api/defaults` constraint metadata conflicts with rules/schema | REST / config | `server/src/app.ts` publishes `totalSlots: { note: 'rows * columns <= 48' }` | Aligned with RULES.md and shared schema | `[docs/api] Align /api/defaults totalSlots metadata with RULES.md and shared schema` | ✅ RESOLVED |
| OpenAPI output is not rich enough for external client codegen | REST / OpenAPI | `docs/api/openapi.json` publishes complete schemas for all REST routes | OpenAPI schemas fully populated for client codegen | `[openapi] Publish response schemas and shared components for public client consumption` | ✅ RESOLVED |
| Public event-envelope contract is not clearly published as one consumer-facing artifact | event model | `shared/src/schema.ts` & TASK-250 semantic events normalize cause tags and envelopes | Published schemas cover all public gameplay events | `[events] Publish a documented consumer-facing JSON schema for public event envelopes` | ✅ RESOLVED |
| Terminology drifts between glossary and state schema | rules / state shape | `GLOSSARY.md` defines `Graveyard`, while `shared/src/schema.ts` exposes `discardPile` | Native UI uses `discardPile` wire-contract term | `[docs] Reconcile glossary terminology with shared state field names (graveyard vs discardPile)` | ✅ INFORMATIONAL |
| Match-parameter overrides are normalized instead of clearly rejected | REST / websocket / match lifecycle | `server/src/match.ts` `resolveMatchParams` clamps `maxHandSize` and rewrites `initialDraw` formula | Documented server parameter normalization | `[contract] Define whether invalid CreateMatchParamsPartial values are rejected or normalized` | ✅ INFORMATIONAL |
| Runtime version and rules spec version are easy to conflate | docs / versioning | `shared/src/schema.ts` exports `SCHEMA_VERSION` while match/rules `specVersion` is `1.0` | `_meta.versions` clearly separates schema and gameplay spec versions | `[docs] Clarify SCHEMA_VERSION vs match specVersion for external clients` | ✅ INFORMATIONAL |
